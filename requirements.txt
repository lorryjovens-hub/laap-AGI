# ── LAAP 运行时核心依赖 ──────────────────────────────────
# 安装: pip install -r requirements.txt
# Python 3.11+ required (推荐 3.13)
#
# ╔══════════════════════════════════════════════════════╗
# ║  层级说明:                                          ║
# ║  CORE    = 必须安装，否则 LAAP 无法启动              ║
# ║  OPTIONAL= 不安装也能启动，但对应模块不可用           ║
# ║  DEV     = 仅开发/调试需要                           ║
# ╚══════════════════════════════════════════════════════╝

# ── CORE: 认知引擎运行时 ─────────────────────────────────
flask>=3.0.0
requests>=2.31.0
numpy>=1.24.0
aiohttp>=3.9.0

# ── OPTIONAL: 记忆与语义引擎 ────────────────────────────
# sentence-transformers        # 语义记忆嵌入 (~500MB 模型)
# chromadb>=0.4.0              # 向量数据库（替换 simple JSON 存储）
# faiss-cpu>=1.7.0             # 高效向量检索

# ── OPTIONAL: 量子推理引擎 ─────────────────────────────
# scipy>=1.11.0                # 量子态演化
# scikit-learn>=1.3.0          # QRE 聚类与降维

# ── OPTIONAL: 本地 LLM 推理 ────────────────────────────
# torch>=2.0.0                 # PyTorch（CUDA / CPU）
# transformers>=4.36.0         # HuggingFace 模型加载
# accelerate>=0.25.0           # 分布式推理

# ── OPTIONAL: API 网关 ─────────────────────────────────
# websockets>=12.0             # WebSocket 网关
# paho-mqtt>=1.6.0             # MQTT 消息总线

# ── OPTIONAL: TTS / 表达层 ─────────────────────────────
# edge-tts>=6.0.0              # 免费 TTS

# ── DEV: 测试与调试 ────────────────────────────────────
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
